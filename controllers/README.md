# Controllers

Controller plugins will be added in M4. Each trusted plugin will be a directory containing
`controller.py`, `config.toml`, and optional helpers or assets. Challenge configuration remains
outside this directory and is read-only from a Controller's perspective.
