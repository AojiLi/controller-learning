# Benchmark Evidence

Only reviewed, versioned benchmark evidence belongs here. Ad-hoc local output goes in
`benchmarks/local/`, which is ignored by Git. M2 will produce `v0.1/gpu_report.json` after the
required 1/64/256/1024-world measurements pass.
